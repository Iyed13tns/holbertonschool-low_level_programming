#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
for (ch= ‘A’; ch<= ‘Z’; c++)
putchar(ch);
{
putchar(c) ;

for (c= ‘a’; ch<= ‘z’; c++)
}
putchar(c);
}
putchar('\n');

return (0)
}
